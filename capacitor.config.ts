import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fgw.biorhythms',
  appName: 'biorhythms',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
